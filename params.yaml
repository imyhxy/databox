# Parameters for databox/segmentation/cvat_xml_to_ultralytics_instance.py.
# Input and output roots remain explicit in dvc.yaml; conversion choices are
# tracked by DVC here.
cvat_xml_to_ultralytics_instance:
  categories:
    - vehicle
  category_map: {}
